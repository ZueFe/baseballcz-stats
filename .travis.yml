language: python

python:
  "3.6"

addons:
  firefox: latest

install:
  pip install -r remote/requirements.txt

before_script:
  "export DISPLAY=:99.0"
  "sh -e /etc/init.d/xvfb start"
  sleep 3
  fluxbox >/dev/null 2>&1 &

script:
  pytest
